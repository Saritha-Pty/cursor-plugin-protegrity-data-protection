Remove MCP runner and switch skills to use Protegrity HTTP APIs.

Summary of changes:
- Neutralized MCP runner configuration (plugins/protegrity-data-protection/mcp.json -> {}).
- Documentation: updated README.md and PREREQUISITES.md to remove MCP-runner instructions and document API-only usage (Docker services on ports 8580/8581 or DEV_EDITION_* env vars).
- Added a small JS api-wrapper (plugins/protegrity-data-protection/skills/api-wrapper.js) for skills to call classification and semantic guardrail HTTP endpoints configured in plugins/protegrity-data-protection/config.json.

Notes:
- Skills should now call the HTTP endpoints or import the api-wrapper. If any code still invokes the MCP runner, that should be refactored to use the wrapper or direct HTTP calls.
- Testing steps are in the PR description; run local Docker services or set DEV_EDITION_* env vars and exercise the plugin commands in Cursor.
